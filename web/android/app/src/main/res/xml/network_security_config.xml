<?xml version="1.0" encoding="utf-8"?>
<!--
  A partir do Android 9 o tráfego HTTP sem TLS é bloqueado por padrão, e é bom
  que seja. Aqui a exceção é aberta SÓ para o servidor de desenvolvimento na
  rede local — todo o resto continua exigindo HTTPS.

  Se o IP da sua máquina mudar (o roteador dá outro por DHCP), troque o
  endereço abaixo e gere o APK de novo. Em produção, com a API em HTTPS,
  este arquivo pode sair junto com a linha `networkSecurityConfig` do
  AndroidManifest.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.100.23</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
